#include<bits/stdc++.h>   
#include<string.h>

using namespace std;

int main(){
    int num_mes, m;

    char mes[10];

    scanf("%d", &num_mes);

    m = num_mes;

    if(m==1){
        strcpy(mes, "January");

        printf("%s\n", mes);

        return 0;
    }

    if(m==2){
        strcpy(mes, "February");

        printf("%s\n", mes);

        return 0;
    }

    if(m==3){
        strcpy(mes, "March");

        printf("%s\n", mes);

        return 0;
    }

    if(m==4){
        strcpy(mes, "April");

        printf("%s\n", mes);

        return 0;
    }

    if(m==5){
        strcpy(mes, "May");

        printf("%s\n", mes);

        return 0;
    }

    if(m==6){
        strcpy(mes, "June");

        printf("%s\n", mes);

        return 0;
    }

    if(m==7){
        strcpy(mes, "July");

        printf("%s\n", mes);

        return 0;
    }

    if(m==8){
        strcpy(mes, "August");

        printf("%s\n", mes);

        return 0;
    }

    if(m==9){
        strcpy(mes, "September");

        printf("%s\n", mes);

        return 0;
    }

    if(m==10){
        strcpy(mes, "October");

        printf("%s\n", mes);

        return 0;
    }

    if(m==11){
        strcpy(mes, "November");

        printf("%s\n", mes);

        return 0;
    }

    if(m==12){
        strcpy(mes, "December");

        printf("%s\n", mes);

        return 0;
    }

    return 0;
    
}